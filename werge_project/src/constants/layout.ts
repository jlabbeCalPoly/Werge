/**
 * Aspect ratios (given in height:width) for components
 */
export const AspectRatios = {
    keyboardAspectRatio: 0.4,
    gridTileAspectRatio: 1
}

/**
 * The ratios used for calculating gap sizing
 */
export const GapRatios = {
    gapRatio: 0.02
}

/**
 * The maximum height ratios that components are allowed to take up on the screen
 */
export const MaxHeightRatios = {
    keyboardHeightRatio: 0.3,
    gridTileHeightRatio: 0.5
}

/**
 * The maximum amount of items that can be found in a row for a component, used to determine sizing
 */
export const MaxItems = {
    keyboardMaxItems: 10,
    gridTileMaxItems: 6
}