/**
 * Used to denote the sizing values required by Tiles
 * 
 * @field tileSize The width and height for the Tile
 * @field fontSize The fontSize of the character in each Tile
 */
export interface TileSizingBundle {
    tileSize: number,
    fontSize: number
}
