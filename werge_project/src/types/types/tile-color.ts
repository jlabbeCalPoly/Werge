/**
 * Used to denote the data found within a TileColor
 * 
 * @field opaque The opaque version of the color
 * @field transparent The semi-transparent version of the color
 */
export type TileColor = {
    opaque: string
    transparent: string
}
