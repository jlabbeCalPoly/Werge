/**
 * Used to denote the start and end opacities for Tile opacity animations
 * 
 * @field start The start opacity
 * @field end The end opacity
 */
export type TileOpacity = {
    start: number,
    end: number
}
